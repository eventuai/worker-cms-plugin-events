<div class="flex items-center justify-between gap-4 mb-4">
  <div>
    <a class="inline-block text-xs text-gray-500 mb-1 hover:text-indigo-700" href="{{ backHref }}">← Guest lists</a>
    <h1 class="text-2xl font-bold text-gray-900">{{ title | default: "New guest list" }}</h1>
  </div>
</div>
<div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6" style="max-width:36rem">
  <form method="post" action="{{ action }}">
    {% if selectedEventId %}
      <input type="hidden" name="event_id" value="{{ selectedEventId }}">
    {% else %}
      <label class="block mb-3"><span class="block text-sm text-gray-700 mb-1">Event</span>
        <select class="block w-full px-3 py-2 border border-gray-300 rounded-lg text-sm" name="event_id" required>
          <option value="">Select an event</option>
          {% for event in events %}
            <option value="{{ event.id }}"{% if event.selected %} selected{% endif %}>{{ event.name }}</option>
          {% endfor %}
        </select>
      </label>
    {% endif %}
    <label class="block mb-3"><span class="block text-sm text-gray-700 mb-1">List name</span><input class="block w-full px-3 py-2 border border-gray-300 rounded-lg text-sm" name="name" required placeholder="VIP guests"></label>
    <input type="hidden" name="allow_checkin" value="yes">
    <button class="inline-flex h-10 items-center justify-center gap-2 rounded-lg border border-transparent bg-indigo-600 px-4 text-sm font-semibold text-white hover:bg-indigo-700 cursor-pointer" type="submit" title="Create guest list" aria-label="Create guest list">
      <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><use href="/assets/icons.svg#plus"></use></svg>
      <span class="hidden lg:inline">Create guest list</span>
    </button>
  </form>
</div>
