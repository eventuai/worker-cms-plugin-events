<div class="flex items-center justify-between gap-4 mb-4">
  <div>
    <a class="inline-block text-xs text-gray-500 mb-1 hover:text-indigo-700" href="{{ backHref | escape }}">← {{ eventName | escape }}</a>
    <h1 class="text-2xl font-bold text-gray-900">All guests</h1>
    <p class="mt-1 text-sm text-gray-500"><span data-table-filter-count="guests">{{ filteredCount }}</span> of {{ totalCount }} guest{% if totalCount != 1 %}s{% endif %} across every list</p>
  </div>
  <div class="flex flex-wrap gap-2">
    <a class="inline-flex h-10 items-center justify-center gap-2 rounded-lg border border-gray-300 bg-white px-4 text-sm font-semibold text-gray-700 hover:bg-gray-50" title="Export CSV" aria-label="Export CSV" href="{{ exportHref | escape }}">
      <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><use href="/assets/icons.svg#download"></use></svg>
      <span class="hidden lg:inline">Export CSV</span>
    </a>
  </div>
</div>
{% if flash != blank %}<p class="mb-4 rounded-lg bg-gray-50 border border-gray-200 px-4 py-3 text-sm text-gray-700">{{ flash | escape }}</p>{% endif %}
<div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6 mb-4">
  <form class="flex flex-wrap items-center gap-3" method="get" action="{{ listHref | escape }}" data-table-filter-form data-table-filter-target="[data-table-filter='guests']" data-table-filter-count-target="[data-table-filter-count='guests']">
    {% if selectedCustomFieldKey != blank %}<input type="hidden" name="cf" value="{{ selectedCustomFieldKey | escape }}">{% endif %}
    <input class="block px-3 py-2 border border-gray-300 rounded-lg text-sm" style="max-width:22rem" name="q" value="{{ q | escape }}" placeholder="Search name, email, phone or guest ID">
    <select class="block px-3 py-2 border border-gray-300 rounded-lg text-sm" name="status">
      <option value="">All statuses</option>
      {% for status in statuses %}<option value="{{ status | escape }}"{% if status == selectedStatus %} selected{% endif %}>{{ status | escape }}</option>{% endfor %}
    </select>
    <select class="block px-3 py-2 border border-gray-300 rounded-lg text-sm" name="color">
      <option value="">All color tags</option>
      <option value="none"{% if selectedColor == 'none' %} selected{% endif %}>No color tag</option>
      {% for color in colorOptions %}<option value="{{ color.value | escape }}"{% if color.selected %} selected{% endif %}>{{ color.label | escape }}</option>{% endfor %}
    </select>
    <button class="inline-flex h-10 w-10 shrink-0 items-center justify-center rounded-lg border border-gray-300 bg-white text-gray-700 hover:bg-gray-50" type="submit" title="Search" aria-label="Search guests">
      <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><use href="/assets/icons.svg#search"></use></svg>
    </button>
    <a class="text-sm font-semibold text-gray-500 hover:text-indigo-700" href="{{ listHref | escape }}" data-table-filter-clear{% if q == blank and selectedStatus == blank and selectedColor == blank %} hidden{% endif %}>Clear</a>
  </form>
</div>
{% render "guest-table",
  guests: guests,
  statuses: statuses,
  listHref: listHref,
  q: q,
  selectedStatus: selectedStatus,
  selectedColor: selectedColor,
  hasEdm: hasEdm,
  showListColumn: true,
  hasCustomFields: hasCustomFields,
  customFieldOptions: customFieldOptions,
  selectedCustomFieldKey: selectedCustomFieldKey,
  emptyMessage: "No guests match this filter."
%}
