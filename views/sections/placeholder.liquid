<div class="flex items-center justify-between gap-4 mb-4"><div><h1 class="text-2xl font-bold text-gray-900">{{ title }}</h1><p class="mt-1 text-sm text-gray-500">{{ "events.views.placeholder.events_suite" | t }}</p></div></div>
<div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
  <p class="text-sm text-gray-700">{{ note }}</p>
  <p class="text-sm text-gray-500" style="margin-top:.75rem">{{ "events.views.placeholder.blueprints_and_blocks_for_this_section_are_already_registered_author_rec" | t }}</p>
</div>
