<div class="flex items-center justify-between gap-4 mb-4"><div><h1 class="text-2xl font-bold text-gray-900">{{ title | escape }}</h1><p class="mt-1 text-sm text-gray-500">Events Suite</p></div></div>
<div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
  <p class="text-sm text-gray-700">{{ note | escape }}</p>
  <p class="text-sm text-gray-500" style="margin-top:.75rem">Blueprints and blocks for this section are already registered — author records in the CMS editor.</p>
</div>
